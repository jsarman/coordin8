// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               v7.36.1
// source: coordin8/lease.proto

/* eslint-disable */
import {
  type CallOptions,
  ChannelCredentials,
  Client,
  type ClientOptions,
  ClientReadableStream,
  type ClientUnaryCall,
  handleServerStreamingCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import Long from "long";
import _m0 from "protobufjs/minimal";
import { Empty } from "../google/protobuf/empty";
import { Timestamp } from "../google/protobuf/timestamp";

export const protobufPackage = "coordin8";

export enum ReclaimReason {
  EXPIRED = 0,
  CANCELLED = 1,
  UNRECOGNIZED = -1,
}

export function reclaimReasonFromJSON(object: any): ReclaimReason {
  switch (object) {
    case 0:
    case "EXPIRED":
      return ReclaimReason.EXPIRED;
    case 1:
    case "CANCELLED":
      return ReclaimReason.CANCELLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ReclaimReason.UNRECOGNIZED;
  }
}

export function reclaimReasonToJSON(object: ReclaimReason): string {
  switch (object) {
    case ReclaimReason.EXPIRED:
      return "EXPIRED";
    case ReclaimReason.CANCELLED:
      return "CANCELLED";
    case ReclaimReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface GrantRequest {
  resourceId: string;
  ttlSeconds: number;
}

export interface RenewRequest {
  leaseId: string;
  ttlSeconds: number;
}

export interface RenewAllRequest {
  leases: RenewAllRequest_Item[];
}

export interface RenewAllRequest_Item {
  leaseId: string;
  ttlSeconds: number;
}

export interface RenewAllResponse {
  results: RenewAllResponse_Result[];
}

export interface RenewAllResponse_Result {
  leaseId: string;
  /** Set on success; absent (and error_message set) on failure. */
  lease:
    | Lease
    | undefined;
  /**
   * Empty on success. "not_found" / "expired" / other on failure —
   * mirrors the Renew RPC's per-call error mapping, just inline
   * instead of as a gRPC status per item.
   */
  errorMessage: string;
}

export interface CancelRequest {
  leaseId: string;
}

export interface WatchExpiryRequest {
  /** Empty = watch all expirations. Set resource_id to watch a specific one. */
  resourceId: string;
}

export interface Lease {
  leaseId: string;
  resourceId: string;
  grantedAt: Date | undefined;
  expiresAt:
    | Date
    | undefined;
  /**
   * The TTL actually granted by the server (may be less than requested).
   * This is the authoritative value for scheduling renewal — derive "when
   * to renew" from ttl_seconds (a duration), never from comparing
   * expires_at against the holder's own clock, which can be skewed
   * relative to the grantor's. expires_at is advisory/observational only.
   * u64::MAX = FOREVER (no expiry). See LEASE_FOREVER in coordin8-core.
   */
  ttlSeconds: number;
  /**
   * Where to renew/cancel this lease: the grantor's own address. Every
   * service that grants leases mounts LeaseService on its own port, so a
   * holder dials grantor_host:grantor_port directly rather than assuming
   * one well-known LeaseMgr location. Mirrors Jini's LandlordLease, which
   * carries a reference to its grantor so a holder never needs prior
   * knowledge of who granted a lease it holds.
   */
  grantorHost: string;
  grantorPort: number;
}

export interface ExpiryEvent {
  leaseId: string;
  resourceId: string;
  expiredAt: Date | undefined;
  reason: ReclaimReason;
}

function createBaseGrantRequest(): GrantRequest {
  return { resourceId: "", ttlSeconds: 0 };
}

export const GrantRequest = {
  encode(message: GrantRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.resourceId !== "") {
      writer.uint32(10).string(message.resourceId);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(16).uint64(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GrantRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGrantRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.resourceId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GrantRequest {
    return {
      resourceId: isSet(object.resourceId) ? globalThis.String(object.resourceId) : "",
      ttlSeconds: isSet(object.ttlSeconds) ? globalThis.Number(object.ttlSeconds) : 0,
    };
  },

  toJSON(message: GrantRequest): unknown {
    const obj: any = {};
    if (message.resourceId !== "") {
      obj.resourceId = message.resourceId;
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GrantRequest>, I>>(base?: I): GrantRequest {
    return GrantRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GrantRequest>, I>>(object: I): GrantRequest {
    const message = createBaseGrantRequest();
    message.resourceId = object.resourceId ?? "";
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseRenewRequest(): RenewRequest {
  return { leaseId: "", ttlSeconds: 0 };
}

export const RenewRequest = {
  encode(message: RenewRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(16).uint64(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RenewRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRenewRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.leaseId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RenewRequest {
    return {
      leaseId: isSet(object.leaseId) ? globalThis.String(object.leaseId) : "",
      ttlSeconds: isSet(object.ttlSeconds) ? globalThis.Number(object.ttlSeconds) : 0,
    };
  },

  toJSON(message: RenewRequest): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RenewRequest>, I>>(base?: I): RenewRequest {
    return RenewRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RenewRequest>, I>>(object: I): RenewRequest {
    const message = createBaseRenewRequest();
    message.leaseId = object.leaseId ?? "";
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseRenewAllRequest(): RenewAllRequest {
  return { leases: [] };
}

export const RenewAllRequest = {
  encode(message: RenewAllRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.leases) {
      RenewAllRequest_Item.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RenewAllRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRenewAllRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.leases.push(RenewAllRequest_Item.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RenewAllRequest {
    return {
      leases: globalThis.Array.isArray(object?.leases)
        ? object.leases.map((e: any) => RenewAllRequest_Item.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RenewAllRequest): unknown {
    const obj: any = {};
    if (message.leases?.length) {
      obj.leases = message.leases.map((e) => RenewAllRequest_Item.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RenewAllRequest>, I>>(base?: I): RenewAllRequest {
    return RenewAllRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RenewAllRequest>, I>>(object: I): RenewAllRequest {
    const message = createBaseRenewAllRequest();
    message.leases = object.leases?.map((e) => RenewAllRequest_Item.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRenewAllRequest_Item(): RenewAllRequest_Item {
  return { leaseId: "", ttlSeconds: 0 };
}

export const RenewAllRequest_Item = {
  encode(message: RenewAllRequest_Item, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(16).uint64(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RenewAllRequest_Item {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRenewAllRequest_Item();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.leaseId = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.uint64() as Long);
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RenewAllRequest_Item {
    return {
      leaseId: isSet(object.leaseId) ? globalThis.String(object.leaseId) : "",
      ttlSeconds: isSet(object.ttlSeconds) ? globalThis.Number(object.ttlSeconds) : 0,
    };
  },

  toJSON(message: RenewAllRequest_Item): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RenewAllRequest_Item>, I>>(base?: I): RenewAllRequest_Item {
    return RenewAllRequest_Item.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RenewAllRequest_Item>, I>>(object: I): RenewAllRequest_Item {
    const message = createBaseRenewAllRequest_Item();
    message.leaseId = object.leaseId ?? "";
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseRenewAllResponse(): RenewAllResponse {
  return { results: [] };
}

export const RenewAllResponse = {
  encode(message: RenewAllResponse, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.results) {
      RenewAllResponse_Result.encode(v!, writer.uint32(10).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RenewAllResponse {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRenewAllResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.results.push(RenewAllResponse_Result.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RenewAllResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => RenewAllResponse_Result.fromJSON(e))
        : [],
    };
  },

  toJSON(message: RenewAllResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => RenewAllResponse_Result.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RenewAllResponse>, I>>(base?: I): RenewAllResponse {
    return RenewAllResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RenewAllResponse>, I>>(object: I): RenewAllResponse {
    const message = createBaseRenewAllResponse();
    message.results = object.results?.map((e) => RenewAllResponse_Result.fromPartial(e)) || [];
    return message;
  },
};

function createBaseRenewAllResponse_Result(): RenewAllResponse_Result {
  return { leaseId: "", lease: undefined, errorMessage: "" };
}

export const RenewAllResponse_Result = {
  encode(message: RenewAllResponse_Result, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    if (message.lease !== undefined) {
      Lease.encode(message.lease, writer.uint32(18).fork()).ldelim();
    }
    if (message.errorMessage !== "") {
      writer.uint32(26).string(message.errorMessage);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): RenewAllResponse_Result {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRenewAllResponse_Result();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.leaseId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.lease = Lease.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.errorMessage = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RenewAllResponse_Result {
    return {
      leaseId: isSet(object.leaseId) ? globalThis.String(object.leaseId) : "",
      lease: isSet(object.lease) ? Lease.fromJSON(object.lease) : undefined,
      errorMessage: isSet(object.errorMessage) ? globalThis.String(object.errorMessage) : "",
    };
  },

  toJSON(message: RenewAllResponse_Result): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.lease !== undefined) {
      obj.lease = Lease.toJSON(message.lease);
    }
    if (message.errorMessage !== "") {
      obj.errorMessage = message.errorMessage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RenewAllResponse_Result>, I>>(base?: I): RenewAllResponse_Result {
    return RenewAllResponse_Result.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RenewAllResponse_Result>, I>>(object: I): RenewAllResponse_Result {
    const message = createBaseRenewAllResponse_Result();
    message.leaseId = object.leaseId ?? "";
    message.lease = (object.lease !== undefined && object.lease !== null) ? Lease.fromPartial(object.lease) : undefined;
    message.errorMessage = object.errorMessage ?? "";
    return message;
  },
};

function createBaseCancelRequest(): CancelRequest {
  return { leaseId: "" };
}

export const CancelRequest = {
  encode(message: CancelRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): CancelRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancelRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.leaseId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CancelRequest {
    return { leaseId: isSet(object.leaseId) ? globalThis.String(object.leaseId) : "" };
  },

  toJSON(message: CancelRequest): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CancelRequest>, I>>(base?: I): CancelRequest {
    return CancelRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CancelRequest>, I>>(object: I): CancelRequest {
    const message = createBaseCancelRequest();
    message.leaseId = object.leaseId ?? "";
    return message;
  },
};

function createBaseWatchExpiryRequest(): WatchExpiryRequest {
  return { resourceId: "" };
}

export const WatchExpiryRequest = {
  encode(message: WatchExpiryRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.resourceId !== "") {
      writer.uint32(10).string(message.resourceId);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): WatchExpiryRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWatchExpiryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.resourceId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WatchExpiryRequest {
    return { resourceId: isSet(object.resourceId) ? globalThis.String(object.resourceId) : "" };
  },

  toJSON(message: WatchExpiryRequest): unknown {
    const obj: any = {};
    if (message.resourceId !== "") {
      obj.resourceId = message.resourceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WatchExpiryRequest>, I>>(base?: I): WatchExpiryRequest {
    return WatchExpiryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WatchExpiryRequest>, I>>(object: I): WatchExpiryRequest {
    const message = createBaseWatchExpiryRequest();
    message.resourceId = object.resourceId ?? "";
    return message;
  },
};

function createBaseLease(): Lease {
  return {
    leaseId: "",
    resourceId: "",
    grantedAt: undefined,
    expiresAt: undefined,
    ttlSeconds: 0,
    grantorHost: "",
    grantorPort: 0,
  };
}

export const Lease = {
  encode(message: Lease, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    if (message.resourceId !== "") {
      writer.uint32(18).string(message.resourceId);
    }
    if (message.grantedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.grantedAt), writer.uint32(26).fork()).ldelim();
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(34).fork()).ldelim();
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(40).uint64(message.ttlSeconds);
    }
    if (message.grantorHost !== "") {
      writer.uint32(50).string(message.grantorHost);
    }
    if (message.grantorPort !== 0) {
      writer.uint32(56).uint32(message.grantorPort);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Lease {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLease();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.leaseId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.resourceId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.grantedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.uint64() as Long);
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.grantorHost = reader.string();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.grantorPort = reader.uint32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Lease {
    return {
      leaseId: isSet(object.leaseId) ? globalThis.String(object.leaseId) : "",
      resourceId: isSet(object.resourceId) ? globalThis.String(object.resourceId) : "",
      grantedAt: isSet(object.grantedAt) ? fromJsonTimestamp(object.grantedAt) : undefined,
      expiresAt: isSet(object.expiresAt) ? fromJsonTimestamp(object.expiresAt) : undefined,
      ttlSeconds: isSet(object.ttlSeconds) ? globalThis.Number(object.ttlSeconds) : 0,
      grantorHost: isSet(object.grantorHost) ? globalThis.String(object.grantorHost) : "",
      grantorPort: isSet(object.grantorPort) ? globalThis.Number(object.grantorPort) : 0,
    };
  },

  toJSON(message: Lease): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.resourceId !== "") {
      obj.resourceId = message.resourceId;
    }
    if (message.grantedAt !== undefined) {
      obj.grantedAt = message.grantedAt.toISOString();
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    if (message.grantorHost !== "") {
      obj.grantorHost = message.grantorHost;
    }
    if (message.grantorPort !== 0) {
      obj.grantorPort = Math.round(message.grantorPort);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Lease>, I>>(base?: I): Lease {
    return Lease.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Lease>, I>>(object: I): Lease {
    const message = createBaseLease();
    message.leaseId = object.leaseId ?? "";
    message.resourceId = object.resourceId ?? "";
    message.grantedAt = object.grantedAt ?? undefined;
    message.expiresAt = object.expiresAt ?? undefined;
    message.ttlSeconds = object.ttlSeconds ?? 0;
    message.grantorHost = object.grantorHost ?? "";
    message.grantorPort = object.grantorPort ?? 0;
    return message;
  },
};

function createBaseExpiryEvent(): ExpiryEvent {
  return { leaseId: "", resourceId: "", expiredAt: undefined, reason: 0 };
}

export const ExpiryEvent = {
  encode(message: ExpiryEvent, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.leaseId !== "") {
      writer.uint32(10).string(message.leaseId);
    }
    if (message.resourceId !== "") {
      writer.uint32(18).string(message.resourceId);
    }
    if (message.expiredAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiredAt), writer.uint32(26).fork()).ldelim();
    }
    if (message.reason !== 0) {
      writer.uint32(32).int32(message.reason);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ExpiryEvent {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseExpiryEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.leaseId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.resourceId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.expiredAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ExpiryEvent {
    return {
      leaseId: isSet(object.leaseId) ? globalThis.String(object.leaseId) : "",
      resourceId: isSet(object.resourceId) ? globalThis.String(object.resourceId) : "",
      expiredAt: isSet(object.expiredAt) ? fromJsonTimestamp(object.expiredAt) : undefined,
      reason: isSet(object.reason) ? reclaimReasonFromJSON(object.reason) : 0,
    };
  },

  toJSON(message: ExpiryEvent): unknown {
    const obj: any = {};
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.resourceId !== "") {
      obj.resourceId = message.resourceId;
    }
    if (message.expiredAt !== undefined) {
      obj.expiredAt = message.expiredAt.toISOString();
    }
    if (message.reason !== 0) {
      obj.reason = reclaimReasonToJSON(message.reason);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ExpiryEvent>, I>>(base?: I): ExpiryEvent {
    return ExpiryEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ExpiryEvent>, I>>(object: I): ExpiryEvent {
    const message = createBaseExpiryEvent();
    message.leaseId = object.leaseId ?? "";
    message.resourceId = object.resourceId ?? "";
    message.expiredAt = object.expiredAt ?? undefined;
    message.reason = object.reason ?? 0;
    return message;
  },
};

/**
 * LeaseService is mounted by every service that grants leased resources
 * (Registry, Space, EventMgr, TransactionMgr), each on its own address —
 * there is no separate, centralized LeaseMgr. A holder renews/cancels by
 * calling the same service it already has a connection to; see `Lease`'s
 * `grantor_host`/`grantor_port` for the case where it doesn't.
 */
export type LeaseServiceService = typeof LeaseServiceService;
export const LeaseServiceService = {
  grant: {
    path: "/coordin8.LeaseService/Grant",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GrantRequest) => Buffer.from(GrantRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => GrantRequest.decode(value),
    responseSerialize: (value: Lease) => Buffer.from(Lease.encode(value).finish()),
    responseDeserialize: (value: Buffer) => Lease.decode(value),
  },
  renew: {
    path: "/coordin8.LeaseService/Renew",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: RenewRequest) => Buffer.from(RenewRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => RenewRequest.decode(value),
    responseSerialize: (value: Lease) => Buffer.from(Lease.encode(value).finish()),
    responseDeserialize: (value: Buffer) => Lease.decode(value),
  },
  /**
   * Batch renewal — mirrors Jini's `Landlord.renewAll`/`LeaseMap`. A holder
   * tracking many leases from the same grantor (e.g. a Space with
   * thousands of live tuples) renews them all in one round trip instead of
   * one RPC per lease.
   */
  renewAll: {
    path: "/coordin8.LeaseService/RenewAll",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: RenewAllRequest) => Buffer.from(RenewAllRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => RenewAllRequest.decode(value),
    responseSerialize: (value: RenewAllResponse) => Buffer.from(RenewAllResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => RenewAllResponse.decode(value),
  },
  cancel: {
    path: "/coordin8.LeaseService/Cancel",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: CancelRequest) => Buffer.from(CancelRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => CancelRequest.decode(value),
    responseSerialize: (value: Empty) => Buffer.from(Empty.encode(value).finish()),
    responseDeserialize: (value: Buffer) => Empty.decode(value),
  },
  watchExpiry: {
    path: "/coordin8.LeaseService/WatchExpiry",
    requestStream: false,
    responseStream: true,
    requestSerialize: (value: WatchExpiryRequest) => Buffer.from(WatchExpiryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => WatchExpiryRequest.decode(value),
    responseSerialize: (value: ExpiryEvent) => Buffer.from(ExpiryEvent.encode(value).finish()),
    responseDeserialize: (value: Buffer) => ExpiryEvent.decode(value),
  },
} as const;

export interface LeaseServiceServer extends UntypedServiceImplementation {
  grant: handleUnaryCall<GrantRequest, Lease>;
  renew: handleUnaryCall<RenewRequest, Lease>;
  /**
   * Batch renewal — mirrors Jini's `Landlord.renewAll`/`LeaseMap`. A holder
   * tracking many leases from the same grantor (e.g. a Space with
   * thousands of live tuples) renews them all in one round trip instead of
   * one RPC per lease.
   */
  renewAll: handleUnaryCall<RenewAllRequest, RenewAllResponse>;
  cancel: handleUnaryCall<CancelRequest, Empty>;
  watchExpiry: handleServerStreamingCall<WatchExpiryRequest, ExpiryEvent>;
}

export interface LeaseServiceClient extends Client {
  grant(request: GrantRequest, callback: (error: ServiceError | null, response: Lease) => void): ClientUnaryCall;
  grant(
    request: GrantRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Lease) => void,
  ): ClientUnaryCall;
  grant(
    request: GrantRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Lease) => void,
  ): ClientUnaryCall;
  renew(request: RenewRequest, callback: (error: ServiceError | null, response: Lease) => void): ClientUnaryCall;
  renew(
    request: RenewRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Lease) => void,
  ): ClientUnaryCall;
  renew(
    request: RenewRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Lease) => void,
  ): ClientUnaryCall;
  /**
   * Batch renewal — mirrors Jini's `Landlord.renewAll`/`LeaseMap`. A holder
   * tracking many leases from the same grantor (e.g. a Space with
   * thousands of live tuples) renews them all in one round trip instead of
   * one RPC per lease.
   */
  renewAll(
    request: RenewAllRequest,
    callback: (error: ServiceError | null, response: RenewAllResponse) => void,
  ): ClientUnaryCall;
  renewAll(
    request: RenewAllRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RenewAllResponse) => void,
  ): ClientUnaryCall;
  renewAll(
    request: RenewAllRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RenewAllResponse) => void,
  ): ClientUnaryCall;
  cancel(request: CancelRequest, callback: (error: ServiceError | null, response: Empty) => void): ClientUnaryCall;
  cancel(
    request: CancelRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  cancel(
    request: CancelRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: Empty) => void,
  ): ClientUnaryCall;
  watchExpiry(request: WatchExpiryRequest, options?: Partial<CallOptions>): ClientReadableStream<ExpiryEvent>;
  watchExpiry(
    request: WatchExpiryRequest,
    metadata?: Metadata,
    options?: Partial<CallOptions>,
  ): ClientReadableStream<ExpiryEvent>;
}

export const LeaseServiceClient = makeGenericClientConstructor(
  LeaseServiceService,
  "coordin8.LeaseService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): LeaseServiceClient;
  service: typeof LeaseServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(long: Long): number {
  if (long.gt(globalThis.Number.MAX_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (long.lt(globalThis.Number.MIN_SAFE_INTEGER)) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return long.toNumber();
}

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
